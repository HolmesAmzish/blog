0.10.0 2026-08-19
1. overhaul admin UI — new AdminLayout with grouped nav, mobile drawer and theme switcher
2. refactor admin API clients to /api/admin/* (category/tag/picture/auth/user), add llm client and language-aware category fetch
3. backend: make CORS configurable via CORS_ALLOWED_ORIGINS env, fix /api/admin/auth/me to return UserPrincipal, migrate Jackson to tools.jackson
4. public frontend: archive tree click navigates to filtered article list via ?categoryId, list page syncs filter to URL; bundle world map statically and fix header offset
5. deploy & tooling: split nginx into blog-public.conf / blog-admin.conf, rename blog.service/env to blog-spring.*, move HTTP tests to top-level test/http and clean obsolete tests

0.9.2 2026-08-18
1. migrate OAuth server to production (auth.arorms.cn), remove hardcoded localhost fallbacks
2. refactor frontend API client
3. simplify archive page — remove article nodes from tree, show only category structure
4. remove ArchiveTreeNode type (no longer needed)
5. update admin env config to use production OAuth endpoints

0.9.1 2026-08-03
1. splite types packages into public and admin
2. extract graphql request of cloudflare task into a schedule component

0.9.0 2026-07-29
1. split frontend into public and admin monorepo (apps/admin, apps/public, packages/types)
2. refactor frontend build tools from npm to pnpm with workspace support
3. migrate authentication from self-issued JWT to Keycloak OAuth2 Resource Server
4. replace Article.author entity relationship with authorId string (UUID from Keycloak)
5. separate public and admin REST controllers — admin stubs in controllers/admin/
6. replace ApiResponse wrapper with raw ResponseEntity in public controllers

0.8.0 2026-07-22
1. split backend into multi-module Gradle project (`:app` and `:common`)
2. move entities/mappers into `cn.arorms.blog.app.entities` and `cn.arorms.blog.app.mappers`
3. normalize import paths across services and repositories (no more `cn.arorms.blog.backend.*` references)
4. move shared DTOs/requests/responses/enums/exceptions into `:common` module under `cn.arorms.blog.common.*`
5. add `Article.toVo()` mapper for full content view alongside `toSummaryVo()`
6. rewrite `ArticleRepositoryCustomImpl` using QueryDSL — drop all `@Query` annotations in favor of typed DSL
7. expose `findById`, `create`, `update` in `ArticleService` interface and add missing `override` modifiers
8. align `delete` semantics — let JPA's `EmptyResultDataAccessException` surface or pre-check via `existsById`

0.7.0 2026-07-04
1. add three-state theme switching (system/light/dark) with ThemeContext and ThemeSwitcher
2. add FOUC-prevention inline script in index.html to apply theme before React renders
3. configure Tailwind v4 @custom-variant dark to drive dark: utilities from a .dark class on <html>
4. add dark variants across Header, Footer, App shell, LoadingScreen, LanguageSwitcher, ArticleCard, HomePage, ArticleListPage, ArticleDetailPage, ArchivePage, AboutPage
5. add scoped highlight.js token colors for light (github) and dark (github-dark) themes in index.css, replacing the always-dark import bug

0.6.1
1. add TrafficMap component — custom D3 SVG world map with country traffic heatmap
2. add fetchCountryTraffic API endpoint
3. add CountryTraffic type definition
4. add home.trafficDistribution i18n
5. fix ArticleListPage loading skeleton layout mismatch

0.6.0
1. refactor SiteStatisticService to SiteStatisticsService (naming consistency)
2. add Cloudflare GraphQL integration for HTTP request statistics
3. add country traffic tracking with CountryTraffic entity and QueryDSL aggregation
4. add spring-boot-starter-graphql and webflux dependencies

0.5.1
1. code-split routes and vendor chunks to reduce initial bundle size

0.5.0
1. refactor ArticleListPage item hover effect
2. add llm translation api

0.4.0
1. change sql filter from spring data jpa specification to querydsl
2. fix ArticleListPage TypeScript type mismatches (ArticleVo vs ArticleListItem)
3. handle null createdAt in date formatting functions
4. refactor ArticleCard into Swiss Style index row
5. refactor HomePage and ArchivePage for consistency

0.3.1
1. add ai translated tag for article translation

0.3.2
1. fix TypeScript build errors - replace CategoryDTO with CategoryVo, remove unused imports
2. fix AdminCategoriesPage getDescendants null check for category.id
3. remove author field from ArticleDetailPage (not in backend ArticleVo)

0.3.1
1. fix category tree display with proper recursion for nested children
2. add delete article API endpoint (DELETE /api/articles/{id})
3. fix delete button not working on admin articles page (missing onClick handler)
4. fix category display showing undefined (CategoryVo uses flat name, not names map)
5. add language parameter to category APIs for i18n support
6. fix useCategories hook not passing language to API
7. add language support for category tree API
8. fix archive page language switch causing ECharts removeChild error
9. change archive page navigation from window.location to React Router navigate
10. add i18n for uncategorized label in archive page
11. fix AdminArticlesPage category filter showing undefined

0.3.0
1. refactor article to use ArticleTranslation for i18n (title/summary/content per language)
2. add JPA Specification for article search (fix keyword type inference bug)
3. add /entity API for admin category/tag management with full entity data
4. refactor Tag to single name field (remove names map/i18n)
5. unify frontend types with backend DTOs (ArticleVo, CategoryVo, TagVo)
6. fix ArticleListPage category filter display
7. fix slug auto-generation to use English title
8. fix ArticleCreateRequest translations format (array not map)

0.2.0
1. add article translation table for internationalization
2. add jsonb for names of tags and categories

0.1.0
1. fix frontend build error
2. add thumbnail for picture manage

0.0.5
1. add admin image manage page

0.0.4
1. add image api

0.0.3
1. add article filter search

0.0.2
1. add CORS config
2. add jwt header authentication
3. add admin management page

0.0.1
1. add kotlin basic api
2. add user authentication
