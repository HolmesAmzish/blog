{
  "permissions": {
    "allow": [
      "Bash(npm run:*)",
      "Bash(./gradlew :backend:build -x test)",
      "Bash(./gradlew -p backend compileKotlin)",
      "Bash(./gradlew dependencies *)"
    ]
  }
}
