### translate Git.md (CH -> EN)
POST {{baseUrl}}/api/admin/llm/translate
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "articleId": 1,
  "title": "Git",
  "summary": "Git 是一个版本控制系统，它可以帮你保管你提交的各种版本以供在你需要的时候回溯旧版。与此同时他还能提供远程仓库同步从而使多个机器的仓库同步。",
  "content": "# 安装与配置\n\n## 下载\n\n**Linux(deb)**\n\n```bash\nsudo apt update\nsudo apt install git\n```\n\n**Windows**\n\n在[Git官网](https://git-scm.com/)上选择Download下载。下载时会默认将git添加至环境变量，安装完成后如果打开命令行窗口，输入`git`返回相应的参数，说明安装成功。\n\n## config 用户设置\n\n打开命令行，设置提交代码仓库时的身份，包括了用户名和联络方式。若不加 `--global` 则仅应用于当前仓库。\n\n```bash\ngit config --global user.name username\n# 如果username包含空格，需要用双引号括起来，例如\"Holme Amzish\"\n\ngit config --global user.email username@email.com\n# --global代表全局，意为对所有仓库的设置都是一样\n\ngit config --list\n# 列出所有设置\n```\n\n# 基础指令\n\n## `init` 仓库初始化\n\n将当前目录初始化为 git 仓库跟踪\n\n```\ngit init\n```\n## commit 初始化与提交\n\n在你需要创建仓库的文件夹根目录按住shift右键，打开命令行，或Git Bash。随后输入`git init`即可在本文件夹中创建仓库，可以看见本文夹多出了.git 这个文件夹，这就是本地仓库所在的位置。\n\n创建完成后，会默认进入主分支也就是**master**，有些版本可能主分支叫做**main**。现在将文件全部添加至暂存区中然后保存至仓库。\n\n```bash\ngit init\n# 初始化仓库\n\ngit add .\n# 添加所有改动的文件至暂存区\n\ngit add file.txt\n# 如果你只想添加某个文件，可以指定这个文件\n\ngit commit -m <message>\n# 将暂存区的文件全部添加至仓库\n# -m 后的参数是每次提交时的信息，如果需要写多行，直接输入git commit后会进入vim来修改\n```\n\n首先按下 i 键进入到插入模式，写完你所要写的信息之后，按下Esc返回命令模式，输入`:wq `(意为write quit) 保存并退出，随后所有文件即被提交至本地仓库中。\n\n更改上一次提交的信息\n\n```bash\ngit commit --amend -m \"new_message\"\n```\n\n```bash\ngit -c user.name=\"gabriel\" -c user.email=\"mizimilias@outlook.com\" commit -m \n```\n\n```bash\ngit -c user.name=\"盛子涵\" -c user.email=\"holmesamzish86@outlook.com\" commit\n```\n\n## diff 查看暂存区的修改\n\n显示当前暂存区代码的对比\n\n```bash\ngit diff [--stat] [--cached]\n```\n\n## log 查看和回溯历史提交\n\n查看历史提交\n\n```bash\ngit log\n# 查看历史提交\n\ngit log --oneline\n\ngit log --pretty=oneline --abbrev-commit --all --graph\n# --pretty=oneline 将每一次记录仅显示一行\n# --abbrev-commit 优化提交信息\n# --all 所有信息\n# --graph 图像化开发线\n```\n\n```bash\ngit reset --<soft/mixed/hard> <commit_hash>\n\n# soft 回退指针，保留暂存区与工作区\n# mixed 回退指针与暂存区，保留工作区\n# hard 回退指针，暂存区与工作区\n```\n\n除了通过哈希代码回退到指定提交，也可以使用指针，HEAD 指针为当前提交，HEAD~1 为前一个提交，以此类推\n\n```bash\ngit reset --soft HEAD~<N>\n```\n\n## branch 分支管理\n\n```bash\ngit branch\n# 查看分支\n\ngit branch <branch name>\n# 创建分支\n```\n\n```bash\ngit checkout <branch name>\n# 切换到分支\n\ngit checkout -b <branch name>\n# 切换分支，如果不存在就创建\n```\n\n```bash\ngit branch -d <branch name>\n# 删除分支\n\ngit branch -D <branch name>\n# 强制删除分支\n```\n\n## merge 合并分支\n\n```bash\ngit merge feature/water\n```\n\n## remote 远程仓库设置\n\n登录[Github主页](https://github.com/)并找到你的代码仓库页面，点击New创建新的代码仓库\n\n之后找到刚创建的git仓库的地址，此处有两个URL地址，分别是HTTPS协议和SSH协议的地址，是远程仓库的链接。这里推荐使用SSH连接，更加安全\n\n需要将本地仓库连接到远程仓库，将本地仓库通过SSH连接的方式连接到远程仓库需要配置SSH密钥。通过命令行进行配置和获取SSH密钥。\n\n然后输入 ls 查看.ssh文件夹的文件，会看到一个以.pub扩展名结尾的SSH公钥文件，通过`cat filename.pub` 查看这个文件并将整个文件的内容复制下来\n\n将这个SSH公钥填入你的Github账号中。重新回到Github主页，点击头像并找到设置的选项，来到设置页面后可以在菜单栏里找到SSH密钥的设置（SSH and GPG keys\n\n点击 New SSH key新建一个SSH密钥配置，名字自定，建议写机器的名字用于区分，下面内容填入刚刚复制过来的SSH公钥。\n\n关联本地仓库和远程仓库\n\n```bash\ngit remote add origin <url>\n# origin是仓库的别称\n# <url>是Github所复制下来的SSH链接\n\ngit remote -v\n# 检查仓库关联的情况\n```\n\n重命名分支与查看和设置分支连接\n\n```bash\ngit remote rename <original_name> <target_name>\ngit remote <get-url/set-url> <remote_name>\n```\n\n取消远程 fetch\n\n```bash\ngit config --unset remote.gitee.fetch\ngit config --unset remote.github.fetch\n```\n\n## push 推送仓库\n\n在本地完成操作并提交给本地仓库后，若需要同远程仓库同步，则需要推送仓库。\n\n```bash\ngit push -u <remote> <branch>\n# 推送本地仓库到远程仓库主分支\n\ngit push --force <remote> <branch>\n```\n\n如果你想要在其他机器，例如你的服务器将源码下载到本地，你可以通过克隆仓库和拉取仓库最新进度来将远程仓库的最新源码同步到服务器中。在这个步骤中不要忘了为服务器配置SSH密钥并添加到Github上。\n\n```bash\ngit clone <url>\n# 克隆远程仓库\n\ngit clone --depth 1 <repostories_url>\n# 只克隆一层\n```\n\n## fetch 获取远程仓库内容\n\n```bash\n# 获取远程仓库但不进行合并\ngit fetch\n```\n\n如果需要更新本地仓库所有分支状态信息，并同步主分支，需要手动合并\n\n```bash\ngit merge origin/main\n```\n\n## pull 拉取分支\n\n`git pull` 会拉取目标分支并直接合并，比如\n\n```bash\n# 同步当前分支的进度到远程的 master\ngit pull origin master\n```\n\n## tag 标签\n\n创建标签\n\n```bash\ngit tag v1.0.0\n# 创建轻量标签\n\ngit tag -a v1.0.0 -m \"release v1.0.0\"\n# 创建附注标签，包含作者、日期和说明\n\ngit tag -a v1.0.0 <commit_hash> -m \"release v1.0.0\"\n# 给指定提交打标签\n```\n\n显示标签\n\n```bash\ngit tag\n# 列出所有标签\n\ngit show v1.0.0\n# 查看某个标签的详细信息\n```\n\n推送标签\n\n```bash\ngit push origin v1.0.0\n# 推送单个标签到远程\n\ngit push origin --tags\n# 推送所有本地标签到远程\n```\n\n删除标签\n\n```bash\ngit tag -d v1.0.0\n# 删除本地标签\n\ngit push origin --delete v1.0.0\n# 删除远程标签\n```\n\n# 特殊文件\n\nGit 带有一些特殊文件用于更好地控制行为\n\n## .gitignore 忽略文件\n\n   用于自动忽略某些文件，在使用 git add . 指令时这些文件不会被添加到暂存区和仓库中\n\n   ```bash\n   keys.properties  # Ignore keys\n   *.log\t\t\t # Ignore all log files\n   src/**/*.log\n   build/\t\t\t # Ignore build directory\n   !.env\t\t\t # Expect the environment directory\n   ```\n\n## .gitkeep 保留空文件夹\n\n   git 默认不追踪空文件夹，在空文件夹下面放上这个即可保证追踪",
  "targetLanguage": "EN"
}
